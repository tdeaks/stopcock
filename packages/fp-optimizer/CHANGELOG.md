# @stopcock/fp-optimizer

## 2.0.0-next.0

### Major Changes

- Initial release. The fused runner bank that previously shipped inside
  `@stopcock/fp` as `@stopcock/fp/fusion/optimized` now lives here as a
  deliberate opt-in install, so an FP-only install no longer carries it.
