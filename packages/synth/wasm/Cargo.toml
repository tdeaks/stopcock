[package]
name = "stopcock_synth_wasm"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
stopcock_dsp_core = { path = "../dsp-core" }
