---
title: fp
description: Portable, typed functional programming for TypeScript
---

import { Aside } from '@astrojs/starlight/components'

```bash
bun add @stopcock/fp
```

`@stopcock/fp` is a focused functional-programming toolkit for TypeScript. It
provides typed data types, eager and lazy collections, algebra, optics,
validation, pattern matching, recursion schemes, and a portable pipeline
compiler without taking ownership of your application runtime.

The root is intentionally small:

```ts
import {
  compile,
  compilePure,
  dual,
  err,
  flow,
  none,
  ok,
  pipe,
  some,
} from '@stopcock/fp'
```

Specialist APIs live in tree-shakeable subpaths:

```ts
import * as A from '@stopcock/fp/array'
import * as Iter from '@stopcock/fp/iter'
import * as O from '@stopcock/fp/option'
import * as R from '@stopcock/fp/result'
import * as V from '@stopcock/fp/validation'
import * as Optic from '@stopcock/fp/optic'
```

## Everyday pipelines

```ts
import { pipe } from '@stopcock/fp'
import * as A from '@stopcock/fp/array'

const activeNames = pipe(
  players,
  A.filter((player) => player.active),
  A.map((player) => player.name),
  A.take(10),
)
```

Public operators support direct and pipe-friendly forms where both are useful:

```ts
const doubled = A.map([1, 2, 3], (value) => value * 2)
const alsoDoubled = pipe([1, 2, 3], A.map((value) => value * 2))
```

Array operations are immutable by default. Explicit `*Into` operations write
into caller-owned storage, while view-named operations are the only APIs
allowed to alias an input.

## Portable compilation

Repeated hot pipelines can be planned once:

```ts
const activeNames = compile(
  A.filter((player: Player) => player.active),
  A.map((player) => player.name),
  A.take(10),
)

activeNames(players)
```

`compile` preserves ordinary callback semantics. `compilePure` additionally
permits rewrites that are valid only when callbacks are pure. Both use
checked-in portable loop templates: the runtime package does not call
`eval`, construct functions from strings, or load a JIT chunk.

```ts
import { explain } from '@stopcock/fp'

console.log(
  explain(
    A.map((value: number) => value * 2),
    A.filter((value) => value > 0),
  ),
)
```

<Aside type="note">
  The optional `@stopcock/fp-compiler` build plugin can lower ordinary
  `pipe`/`flow` call sites at build time. Applications still import only
  `@stopcock/fp`; generated code does not require the compiler at runtime.
</Aside>

## Missing values, failures, and validation

Use `Option` for absence, `Result` for fail-fast recoverable errors, and
`Validation` when independent checks should accumulate all errors.

```ts
import { pipe } from '@stopcock/fp'
import * as O from '@stopcock/fp/option'
import * as R from '@stopcock/fp/result'
import * as V from '@stopcock/fp/validation'

const port = pipe(
  O.fromNullable(process.env.PORT),
  O.map(Number),
  O.filter((value) => Number.isInteger(value) && value > 0),
  O.getOrElse(() => 3000),
)

const parsed = R.tryCatch(
  () => JSON.parse(rawConfig) as Config,
  (cause) => ({ type: 'InvalidJson' as const, cause }),
)

const fields = V.all([
  V.fromPredicate(name, (value) => value.length > 0, () => 'name is required'),
  V.fromPredicate(age, Number.isFinite, () => 'age must be finite'),
] as const)
```

Asynchronous failure belongs in `@stopcock/async/task`; there is deliberately
no competing `AsyncResult` type.

## Lazy sequences

`Iter` is the single lazy synchronous sequence abstraction:

```ts
import { pipe } from '@stopcock/fp'
import * as Iter from '@stopcock/fp/iter'

const squares = pipe(
  Iter.range(1, Infinity),
  Iter.map((value) => value * value),
  Iter.filter((value) => value % 2 === 0),
  Iter.take(5),
  Iter.toArray,
)
```

See [Iter](/api/iter), [Option and Result](/concepts/option-result),
[Validation](/api/validation), [Optics](/api/optic), and the complete
[module catalogue](/api/modules).
