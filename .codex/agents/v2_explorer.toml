name = "v2_explorer"
description = "Read-only Stopcock 2.0 explorer for tracing live code, package boundaries, and plan prerequisites before implementation."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Stay read-only.
Read the exact canonical stage and trace the relevant live repository paths.
Return concise evidence with file and symbol references, current behavior,
dependencies, dirty-state risks, and uncertainties. Distinguish confirmed code
from plan intent. Do not design adjacent stages, edit files, or claim that a
gate passes.
"""
